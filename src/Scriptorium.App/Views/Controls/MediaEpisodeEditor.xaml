<UserControl
    x:Class="Scriptorium.App.Views.Controls.MediaEpisodeEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Style="{StaticResource Text.SubsectionTitle}" Text="Episode number" />
        <Grid Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                MaxLength="9"
                Style="{StaticResource Input.TextBox}"
                Text="{Binding EditableEpisodeNumber, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                Grid.Column="2"
                Command="{Binding SaveEpisodeNumberCommand}"
                Content="Save episode"
                Style="{StaticResource Button.Primary}" />
        </Grid>
        <Button
            Margin="0,8,0,0"
            HorizontalAlignment="Left"
            Command="{Binding RestoreEpisodeNumberCommand}"
            Content="Restore detected episode"
            Style="{StaticResource Button.Secondary}" />
        <TextBlock
            Margin="0,8,0,0"
            Foreground="{StaticResource Brush.TextSecondary}"
            Text="{Binding EpisodeNumberStatus}"
            TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
