<UserControl
    x:Class="Scriptorium.App.Views.Controls.MediaReleaseYearEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Style="{StaticResource Text.SubsectionTitle}" Text="Release year" />
        <Grid Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                MaxLength="4"
                Style="{StaticResource Input.TextBox}"
                Text="{Binding EditableReleaseYear, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                Grid.Column="2"
                Command="{Binding SaveReleaseYearCommand}"
                Content="Save release year"
                Style="{StaticResource Button.Primary}" />
        </Grid>
        <TextBlock
            Margin="0,8,0,0"
            Foreground="{StaticResource Brush.TextSecondary}"
            Text="{Binding ReleaseYearStatus}"
            TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
